#include<stdio.h>

int main(){
	int a,b,c,d,max,sec;
	scanf("%d %d %d %d",&a,&b,&c,&d);
	
	max = a>b?a:b;
	max = max>c?max:d;
	max = max>d?max:0;
	
	sec = a != max?a:0;
	sec = b != max && b>sec?b:sec;
	sec = c != max && c>sec?c:sec;
	sec = d != max && d>sec?d:sec;
	printf("%d",sec);
}
